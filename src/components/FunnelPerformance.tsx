import { EmptyState } from './EmptyState';

export function FunnelPerformance() {
  return (
    <EmptyState
      title="Hiring Funnel Performance"
      description="No funnel stage data source is connected yet."
    />
  );
}
