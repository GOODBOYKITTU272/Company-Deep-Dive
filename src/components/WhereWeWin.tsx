import { EmptyState } from './EmptyState';

export function WhereWeWin() {
  return (
    <EmptyState
      title="Where We Win / Lose"
      description="No performance segmentation data source is connected yet."
    />
  );
}
