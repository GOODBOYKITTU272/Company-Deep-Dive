import { EmptyState } from './EmptyState';

export function SpeedPredictability() {
  return (
    <EmptyState
      title="Speed & Predictability"
      description="No SLA or delivery timing data source is connected yet."
    />
  );
}
