import { EmptyState } from './EmptyState';

export function ActionCenter() {
  return (
    <EmptyState
      title="Action Center"
      description="No outreach task data source is connected yet."
    />
  );
}
