import { EmptyState } from './EmptyState';

export function RoleDemandRadar() {
  return (
    <EmptyState
      title="Role Demand Radar"
      description="No role demand data source is connected yet."
    />
  );
}
